---
// FluxCanvas — Lab #001 C (v2 "深海の漂流"). Raw WebGL2 fragment shader.
//
// Renders a continuous noise field that lerps between two deep cool tones:
//   u_colA = #0C1014 (oklch 0.12 0.02 240) — abyss dark
//   u_colB = #3A4654 (oklch 0.32 0.04 220) — cool muted highlight
// Both colors hue-shift ±15° on a 90s sin cycle, evaluated each frame from
// Date.now() (independent of accumulated u_time so visuals stay phase-stable
// across pause/resume).
//
// Uniforms: u_time, u_resolution, u_intensity, u_colA, u_colB.
//
// Performance discipline:
// - devicePixelRatio clamped to <= 1.5 (avoid retina cost on idle visuals).
// - IntersectionObserver gates the rAF loop — off-screen = no frames.
// - context-lost handler simply stops the loop; the CSS gradient underlay
//   is always painted so the section never goes black.
// - prefers-reduced-motion: render exactly one frame at u_time = 0 and stop.

const fallbackId = 'flux-fallback';
---

<div class="flux-canvas" role="presentation" aria-hidden="true" data-canvas="flux">
  {/* Always-visible fallback. Sits under the canvas; remains the only visible
      layer if WebGL fails to init or the context is lost. */}
  <div class={`flux-canvas__fallback ${fallbackId}`}></div>
  <canvas class="flux-canvas__gl" aria-hidden="true"></canvas>
</div>

<style>
  .flux-canvas {
    position: absolute;
    inset: 0;
  }
  .flux-canvas__fallback {
    position: absolute;
    inset: 0;
    /* v2: matches u_colA → u_colB (#0C1014 → #3A4654). */
    background: linear-gradient(180deg, #0c1014 0%, #3a4654 100%);
  }
  .flux-canvas__gl {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: block;
  }
</style>

<script>
  // Wire each FluxCanvas instance separately so multiple Lab pages would
  // continue to work without coupling.
  const nodes = document.querySelectorAll<HTMLElement>('.flux-canvas');

  const VERT = `#version 300 es
in vec2 a_pos;
void main() { gl_Position = vec4(a_pos, 0.0, 1.0); }`;

  // Simplex-ish noise via classic 2D hash + smooth lerp — light enough to
  // ship without a noise lib while reading as a continuous field.
  // v2: color ramp now mixes between two uniform colors (u_colA, u_colB)
  // that are hue-shifted on the CPU each frame.
  const FRAG = `#version 300 es
precision highp float;
out vec4 outColor;
uniform vec2 u_resolution;
uniform float u_time;
uniform float u_intensity;
uniform vec3 u_colA;
uniform vec3 u_colB;

float hash(vec2 p) {
  p = fract(p * vec2(123.34, 456.21));
  p += dot(p, p + 45.32);
  return fract(p.x * p.y);
}

float noise(vec2 p) {
  vec2 i = floor(p);
  vec2 f = fract(p);
  float a = hash(i);
  float b = hash(i + vec2(1.0, 0.0));
  float c = hash(i + vec2(0.0, 1.0));
  float d = hash(i + vec2(1.0, 1.0));
  vec2 u = f * f * (3.0 - 2.0 * f);
  return mix(mix(a, b, u.x), mix(c, d, u.x), u.y);
}

float fbm(vec2 p) {
  float v = 0.0;
  float amp = 0.5;
  for (int i = 0; i < 4; i++) {
    v += amp * noise(p);
    p *= 2.0;
    amp *= 0.5;
  }
  return v;
}

void main() {
  vec2 uv = gl_FragCoord.xy / u_resolution.xy;
  vec2 p = uv * 2.5;
  // Two slowly-warping noise queries layered for a sense of depth.
  float n1 = fbm(p + vec2(u_time, u_time * 0.7));
  float n2 = fbm(p * 1.6 - vec2(u_time * 0.4, u_time));
  float n = mix(n1, n2, 0.5);

  // Intensity rescales contrast around the midpoint.
  n = 0.5 + (n - 0.5) * (0.5 + u_intensity * 1.2);

  // Dark tonal curve (pow 2.2) keeps shade close to u_colA.
  float k = pow(clamp(n, 0.0, 1.0), 2.2);
  vec3 col = mix(u_colA, u_colB, k);
  outColor = vec4(col, 1.0);
}`;

  // --- Color helpers (sRGB ↔ HSL) for CPU-side hue shift. ---
  type RGB = [number, number, number];
  type HSL = [number, number, number]; // h in degrees, s/l in 0..1

  const hexToRgb = (hex: string): RGB => {
    const h = hex.replace('#', '');
    return [
      parseInt(h.slice(0, 2), 16) / 255,
      parseInt(h.slice(2, 4), 16) / 255,
      parseInt(h.slice(4, 6), 16) / 255,
    ];
  };

  const rgbToHsl = ([r, g, b]: RGB): HSL => {
    const max = Math.max(r, g, b), min = Math.min(r, g, b);
    const l = (max + min) / 2;
    let h = 0, s = 0;
    if (max !== min) {
      const d = max - min;
      s = l > 0.5 ? d / (2 - max - min) : d / (max + min);
      switch (max) {
        case r: h = ((g - b) / d + (g < b ? 6 : 0)); break;
        case g: h = ((b - r) / d + 2); break;
        case b: h = ((r - g) / d + 4); break;
      }
      h *= 60;
    }
    return [h, s, l];
  };

  const hslToRgb = ([h, s, l]: HSL): RGB => {
    h = ((h % 360) + 360) % 360;
    const c = (1 - Math.abs(2 * l - 1)) * s;
    const x = c * (1 - Math.abs(((h / 60) % 2) - 1));
    const m = l - c / 2;
    let r1 = 0, g1 = 0, b1 = 0;
    if (h < 60)       { r1 = c; g1 = x; }
    else if (h < 120) { r1 = x; g1 = c; }
    else if (h < 180) { g1 = c; b1 = x; }
    else if (h < 240) { g1 = x; b1 = c; }
    else if (h < 300) { r1 = x; b1 = c; }
    else              { r1 = c; b1 = x; }
    return [r1 + m, g1 + m, b1 + m];
  };

  // Designer-confirmed base colors and hue-shift params.
  const BASE_A_HSL = rgbToHsl(hexToRgb('#0C1014'));
  const BASE_B_HSL = rgbToHsl(hexToRgb('#3A4654'));
  const HUE_SHIFT_DEG = 15;
  const HUE_PERIOD_MS = 90_000;

  function initOne(host: HTMLElement) {
    const canvas = host.querySelector<HTMLCanvasElement>('.flux-canvas__gl');
    if (!canvas) return;
    const gl = canvas.getContext('webgl2', { antialias: false, powerPreference: 'low-power' });
    if (!gl) {
      canvas.remove(); // fallback gradient stays visible
      return;
    }

    const compile = (type: number, src: string): WebGLShader | null => {
      const sh = gl.createShader(type);
      if (!sh) return null;
      gl.shaderSource(sh, src);
      gl.compileShader(sh);
      if (!gl.getShaderParameter(sh, gl.COMPILE_STATUS)) {
        // eslint-disable-next-line no-console
        console.warn('[FluxCanvas] shader compile failed', gl.getShaderInfoLog(sh));
        return null;
      }
      return sh;
    };

    const vs = compile(gl.VERTEX_SHADER, VERT);
    const fs = compile(gl.FRAGMENT_SHADER, FRAG);
    if (!vs || !fs) { canvas.remove(); return; }

    const prog = gl.createProgram()!;
    gl.attachShader(prog, vs);
    gl.attachShader(prog, fs);
    gl.linkProgram(prog);
    if (!gl.getProgramParameter(prog, gl.LINK_STATUS)) { canvas.remove(); return; }
    gl.useProgram(prog);

    // Full-screen triangle.
    const buf = gl.createBuffer();
    gl.bindBuffer(gl.ARRAY_BUFFER, buf);
    gl.bufferData(gl.ARRAY_BUFFER, new Float32Array([-1, -1, 3, -1, -1, 3]), gl.STATIC_DRAW);
    const loc = gl.getAttribLocation(prog, 'a_pos');
    gl.enableVertexAttribArray(loc);
    gl.vertexAttribPointer(loc, 2, gl.FLOAT, false, 0, 0);

    const uTime = gl.getUniformLocation(prog, 'u_time');
    const uRes = gl.getUniformLocation(prog, 'u_resolution');
    const uIntensity = gl.getUniformLocation(prog, 'u_intensity');
    const uColA = gl.getUniformLocation(prog, 'u_colA');
    const uColB = gl.getUniformLocation(prog, 'u_colB');

    const state = { speed: 0.3, intensity: 0.4, time: 0, visible: false, raf: 0, reduced: false };

    const mql = window.matchMedia('(prefers-reduced-motion: reduce)');
    state.reduced = mql.matches;
    mql.addEventListener?.('change', (e) => { state.reduced = e.matches; if (state.reduced) drawOnce(); });

    function resize() {
      const dpr = Math.min(window.devicePixelRatio || 1, 1.5);
      const w = Math.floor(canvas!.clientWidth * dpr);
      const h = Math.floor(canvas!.clientHeight * dpr);
      if (canvas!.width !== w || canvas!.height !== h) {
        canvas!.width = w;
        canvas!.height = h;
        gl!.viewport(0, 0, w, h);
      }
    }

    function uploadColors() {
      // ±15° hue shift on a 90s sin cycle, evaluated from wall-clock so the
      // drift stays consistent regardless of when rAF resumes after pause.
      const phase = Math.sin((2 * Math.PI * Date.now()) / HUE_PERIOD_MS);
      const shift = phase * HUE_SHIFT_DEG;
      const a = hslToRgb([BASE_A_HSL[0] + shift, BASE_A_HSL[1], BASE_A_HSL[2]]);
      const b = hslToRgb([BASE_B_HSL[0] + shift, BASE_B_HSL[1], BASE_B_HSL[2]]);
      gl!.uniform3f(uColA, a[0], a[1], a[2]);
      gl!.uniform3f(uColB, b[0], b[1], b[2]);
    }

    function draw() {
      resize();
      gl!.uniform1f(uTime, state.time);
      gl!.uniform2f(uRes, canvas!.width, canvas!.height);
      gl!.uniform1f(uIntensity, state.intensity);
      uploadColors();
      gl!.drawArrays(gl!.TRIANGLES, 0, 3);
    }

    function drawOnce() {
      state.time = 0;
      draw();
    }

    function tick() {
      // Match spec: uTime += 0.0008 + speed * 0.012 per frame.
      state.time += 0.0008 + state.speed * 0.012;
      draw();
      state.raf = requestAnimationFrame(tick);
    }

    function start() {
      if (state.raf || state.reduced) { drawOnce(); return; }
      state.raf = requestAnimationFrame(tick);
    }
    function stop() {
      if (state.raf) { cancelAnimationFrame(state.raf); state.raf = 0; }
    }

    if ('IntersectionObserver' in window) {
      const io = new IntersectionObserver(
        (entries) => {
          for (const e of entries) {
            state.visible = e.isIntersecting;
            if (state.visible) start(); else stop();
          }
        },
        { threshold: 0.05 },
      );
      io.observe(host);
    } else {
      start();
    }

    window.addEventListener('resize', () => { if (state.visible) draw(); });

    // Context loss: stop rAF; the CSS fallback under the <canvas> stays visible.
    canvas.addEventListener('webglcontextlost', (e) => { e.preventDefault(); stop(); });

    const frame = host.closest('.experiment-frame');
    frame?.addEventListener('paramchange', (event) => {
      const { name, value } = (event as CustomEvent<{ name: string; value: number }>).detail;
      if (name === 'speed') state.speed = value;
      else if (name === 'intensity') state.intensity = value;
      if (state.reduced) drawOnce();
    });

    // First paint.
    drawOnce();
  }

  nodes.forEach(initOne);
</script>
